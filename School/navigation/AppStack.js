import React, { useState } from 'react';
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    Button,
    TextInput,
    StatusBar,
} from 'react-native';
function First() {
    return (
        <View>
            <Text style={{ flex: 1, textAlign: "center", justifyContent: "center" }}>First Screen</Text>
        </View>
    )
}
export default First
